{
  "name": "verticosys-lp",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "clean": "rimraf dist .astro node_modules/.vite node_modules/.astro",
    "clean:all": "rimraf dist .astro node_modules && npm cache verify",
    "fresh": "npm run clean && npm run build"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "shiki": "^4.3.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
